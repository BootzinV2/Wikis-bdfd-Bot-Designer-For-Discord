INFORMAÇÕES
⚠️ Esse comando foi feito para / (slash commands).

Slash Opções
→ Adicone a seguinte opção ao slash command de atm:

Nome da opção:








Código:

$nomention

$var[user;$findUser[$message[>;usuário]]]
$var[money;moedas]

<@$authorID>

$autor[Saldo de: $username[$var[user]]#$discriminator[$var[user]]]
$authorIcon[$userAvatar[$var[user]]

$description[`$usernickname[$var[user]]` tem $getVar[$var[money];$var[user]] money!]
